<h2>Users <span class="footnote">- CustomerHelper</span></h2>

<ul>
  <% @users.each do |user| %>
    <li><%= CustomerPresenter.new(user, self).info %></li>
  <% end %>
</ul>

<h2>Users <span class="footnote">- BusinessHelper</span></h2>

<ul>
  <% @users.each do |user| %>
    <li><%= BusinessPresenter.new(user, self).info %></li>
  <% end %>
</ul>
